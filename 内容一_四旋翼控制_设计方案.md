# 内容一设计方案：四旋翼无人机控制算法设计与仿真

> 依据《智能机器人系统综合训练》PPT：二选一选**四旋翼**（验收项比机械臂更具体）。  
> 闭环：**建模 → 控制 → 仿真 → 优化**。平台：**MATLAB/Simulink**。  
> 双算法：**串级 PID** + **反步法（Backstepping）**（PPT 点名示例）。

---

## 0. 与 PPT 的一一对应

| PPT 要求 | 本方案落地 |
|---|---|
| 运动学/动力学建模 | Newton–Euler + ZYX 欧拉角，12 状态、4 输入 |
| ≥2 种控制算法 | 串级 PID、反步法；同一被控对象对比 |
| 定位 + 轨迹跟踪 | 定点悬停 + 5 组随机航点平滑过渡 |
| MATLAB 仿真 | Simulink 连续模型，示波器 + 三维轨迹 |
| 悬停/升降/前后/左右 | 5 个基础机动实验 |
| 随机 5 组目标位置 | `rand` 生成航点，限幅后依次跟踪 |
| 误差曲线定位问题 | 位置误差、姿态角、角速度超调 |
| 解决姿态发散 | 内环优先、姿态限幅、积分抗饱和、步长固定 |

机械臂备选见文末附录，不与主线并行实现。

---

## 1. 设计方法

### 1.1 方法总纲

四旋翼是**欠驱动、强耦合、非线性**系统：4 个输入（总推力 \(U_1\)，滚转/俯仰/偏航力矩 \(U_2,U_3,U_4\)）控制 6 自由度。位置通过姿态间接实现。

采用**分层控制**：

```
期望位置 (xd,yd,zd,ψd)
        ↓  外环：位置控制器
期望姿态 (φd,θd) + 总推力 U1
        ↓  内环：姿态控制器
力矩 (U2,U3,U4)
        ↓  控制分配（Mixer）
四旋翼转速 (ω1..ω4)
        ↓  动力学模型
实际位姿 (x,y,z,φ,θ,ψ)
```

原则：

1. **先内环后外环**：姿态不稳则位置必散。
2. **同一被控对象、同一航点、同一指标**对比两种算法。
3. **先无扰再加扰**：先调通再加风扰/质量偏差，解释误差来源。
4. **手绘原理图、公式编辑器写公式**（PPT 明确禁止拷贝原理图）。

### 1.2 建模方法

| 层级 | 内容 | 作用 |
|---|---|---|
| 运动学 | 欧拉角 ↔ 旋转矩阵 \(R\)；欧拉角速度 ↔ 体轴角速度 | 位姿描述 |
| 平移动力学 | \(m\ddot{\mathbf{p}} = R\mathbf{e}_3 U_1 - mg\mathbf{e}_3 - \mathbf{D}_v\dot{\mathbf{p}}\) | 位置/高度 |
| 转动动力学 | \(J\dot{\boldsymbol{\omega}} = -\boldsymbol{\omega}\times J\boldsymbol{\omega} + \boldsymbol{\tau}\) | 姿态 |
| 动力单元 | \(U_1=b\sum\omega_i^2\)，力矩由差速产生 | 控制分配 |
| 简化假设 | 刚体、质量恒定、桨盘气动力用推力/反扭矩系数、小角度可线性化（仅 PID 设计时） | 可仿真、可调参 |

坐标系：

- 惯性系 \(\{E\}\)：东北天或北东地，全程固定一种，报告写清。
- 机体系 \(\{B\}\)：原点质心，\(x_b\) 机头，\(z_b\) 垂直桨盘。
- 构型：优先 **“+” 型**（公式与 PPT 参考一致）；若用 “×” 型须改 Mixer。

状态 \(\mathbf{x}=[x,y,z,\dot x,\dot y,\dot z,\phi,\theta,\psi,p,q,r]^\top\)。

控制 \(\mathbf{u}=[U_1,U_2,U_3,U_4]^\top\)。

“+” 型分配（自绘）：

\[
\begin{aligned}
U_1 &= b(\omega_1^2+\omega_2^2+\omega_3^2+\omega_4^2)\\
U_2 &= b\ell(\omega_4^2-\omega_2^2)\\
U_3 &= b\ell(\omega_3^2-\omega_1^2)\\
U_4 &= d(\omega_2^2+\omega_4^2-\omega_1^2-\omega_3^2)
\end{aligned}
\]

对角电机转向相反，用反扭矩平衡偏航（对应 PPT「反扭矩作用」）。

### 1.3 控制方法（两种）

**算法 A：串级 PID（线性、工程基线）**

- 外环：\(x,y,z\) 各一套 PID → 得到期望加速度 → 反解 \(\phi_d,\theta_d,U_1\)。
- 内环：\(\phi,\theta,\psi\) 各一套 PID（可再串角速度环，共 6 个姿态相关 PID）。
- 优点：结构清楚、调参直观、报告好写。
- 缺点：大倾角/强扰动时线性假设失效，易超调。

**算法 B：反步法（非线性，PPT 示例）**

- 把系统写成严格反馈：位置误差 → 虚拟速度 → 虚拟姿态 → 力矩。
- 每步构造 Lyapunov 函数 \(V_i\)，保证 \(\dot V_i<0\)。
- 优点：大范围稳定有理论依据，跟踪更紧。
- 缺点：虚控量求导麻烦、增益多、实现易数值爆。

对比维度（报告必须有表）：

| 指标 | 定义 | 用途 |
|---|---|---|
| 位置 RMSE | \(\sqrt{\mathrm{mean}(e_x^2+e_y^2+e_z^2)}\) | 精度 |
| 稳态误差 | 到达后 2 s 内 \(\|e\|\) 均值 | 悬停精度 |
| 超调 / 调节时间 | 阶跃高度或航点切换 | 动态品质 |
| 角速度超调 | \(\max\|p,q,r\|\) | 是否接近发散 |
| 控制能量 | \(\int U_i^2\mathrm{d}t\) | 平滑性 |

### 1.4 仿真与优化方法

- 求解器：定步长 `ode4`，步长 `1e-3` s（变步长易把姿态抖成“发散”）。
- 观测：Scope（\(x,y,z,\phi,\theta,\psi,p,q,r,e\)）+ `plot3` 轨迹。
- 可选：Aerospace Blockset 6DOF + FlightGear（加分，非必须）。
- 调参：先内环 P → 加 D → 外环 P → 最后 I；反步增益由小到大。
- 优化闭环：看误差曲线 → 判断内/外环 → 改一组参数 → 复测同一航点。

---

## 2. 设计方式

### 2.1 系统架构（Simulink）

```
Quadrotor_Control.slx
├── Trajectory_Gen      航点 / 机动指令
├── Controller_Switch   1=PID, 2=Backstepping
├── Position_Ctrl
├── Attitude_Ctrl
├── Control_Allocation  U → ω²
├── Plant_6DOF          运动学 + 动力学
├── Sensors_Ideal       先理想全状态；后可加噪声
└── Visualization       Scope / XY Graph / 三维 / 可选 FlightGear
```

两种控制器**只换控制律，不换 Plant**，保证对比公平。

### 2.2 实验设计方式（对应 PPT 操控项）

| 编号 | 实验 | 指令 | 验收看什么 |
|---|---|---|---|
| E1 | 悬停 | \( (0,0,1) \)，ψ=0，20 s | 位置收敛、姿态不振荡 |
| E2 | 升降 | z: 1→2→0.5 m | 高度超调、落地不翻 |
| E3 | 前后 | x: 0→2→0 m，y=0 | 俯仰合理、侧向耦合小 |
| E4 | 左右 | y: 0→2→0 m，x=0 | 滚转合理 |
| E5 | 五航点 | 随机 5 点，逐点到达再切下一目标 | 平滑、无姿态发散 |
| E6 | 抗扰（加分） | E1 上加常值/正弦风扰 | 对比 PID vs 反步 |

E5 航点生成方式（写进报告「方法」）：

```matlab
rng(2026);                         % 可复现
wp = [4*(rand(5,2)-0.5), 0.8+1.2*rand(5,1), zeros(5,1)];
% 列: x,y,z,psi；平面 ±2 m，高度 0.8~2.0 m
```

到达判据：\(\|e_{xyz}\| < 0.08\,\mathrm{m}\) 且保持 1 s，再切下一航点。两点间用**五次多项式或梯形速度**插值，避免阶跃位置指令把姿态打飞。

### 2.3 参数与文件组织

建议物理参数（可按参考机改，但必须在报告列出并手写符号说明）：

| 符号 | 含义 | 初值建议 |
|---|---|---|
| \(m\) | 质量 | 1.0 kg |
| \(g\) | 重力 | 9.81 |
| \(\ell\) | 力臂 | 0.225 m |
| \(I_x,I_y\) | 转动惯量 | 0.01 kg·m² |
| \(I_z\) | 偏航惯量 | 0.02 kg·m² |
| \(b\) | 推力系数 | \(1.0\times10^{-5}\) |
| \(d\) | 反扭矩系数 | \(3.0\times10^{-7}\) |

工程目录：

```
内容一_四旋翼/
  params.m                 % 全部物理/控制参数
  traj_waypoints.m         % 5 航点 + 插值
  Quadrotor_Control.slx
  init.m                   % 加载参数、选算法
  plot_results.m           % RMSE、误差曲线、三维轨迹
  report_figs/             % 导出图
```

### 2.4 报告写法（对应 PPT 报告结构）

| 报告章节 | 写什么 |
|---|---|
| 实验目的和要求 | 抄任务书要点 + 本组量化指标（如 RMSE<0.1 m） |
| 实验原理 | 坐标系、欧拉方程、PID 结构、反步 Lyapunov（**自己画图**） |
| 方法与内容 | 需求、算法思路、系统框图、流程图 |
| 原始记录 | 关键子系统截图、核心公式对应代码、参数表 |
| 结果及分析 | E1–E5 曲线 + 双算法对比表 + 发散问题复盘 |
| 总结与思考 | 问题/解决、分工、展望 |

---

## 3. 设计步骤（按周可执行）

### 阶段 A：建模（约 4 学时）

**A1 需求冻结**

- 选题：四旋翼（书面写清为何不选机械臂：PPT 机动/五航点验收更明确）。
- 量化指标：悬停稳态误差 < 5 cm；航点 RMSE < 10 cm；姿态角不发散（\(|\phi|,|\theta|<35^\circ\)）。

**A2 坐标系与假设**

- 画 \(\{E\},\{B\}\)、“+” 型电机编号（1 前、2 右、3 后、4 左，对角同向）。
- 写清欧拉角顺序 ZYX，避免后面旋转矩阵写反。

**A3 运动学**

- 写 \(R_{B}^{E}(\phi,\theta,\psi)\)。
- 写欧拉角速率与 \( (p,q,r) \) 的变换，标明奇异点 \(\theta=\pm90^\circ\)（仿真限幅避开）。

**A4 动力学**

- 平移、转动方程用公式编辑器录入。
- 推导 Mixer 与逆分配（由 \(U\) 求 \(\omega_i^2\)，加 \(\omega\ge0\) 饱和）。

**A5 搭 Plant**

- Simulink：12 个积分器或 6DOF 模块。
- 开环自检：只给 \(U_1=mg\)，其余为 0，应近似悬停（无扰时缓慢漂可接受）。
- 开环给小 \(U_2\)，应出现滚转。不过此关不要进入闭环。

### 阶段 B：控制（约 5 学时）

**B1 串级 PID**

1. 只闭环高度 + 姿态（xy 开环或锁 0），先把“不翻机”做出来。
2. 姿态内环：先 P，振荡则加 D，稳态差再加很小的 I。
3. \(\phi_d,\theta_d\) 限幅 \(\pm20^\circ\sim\pm30^\circ\)。
4. 再闭环 \(x,y\)：位置误差 → 期望加速度 →

\[
\phi_d \approx \frac{1}{g}(a_x\sin\psi_d - a_y\cos\psi_d),\quad
\theta_d \approx \frac{1}{g}(a_x\cos\psi_d + a_y\sin\psi_d)
\]

（与坐标系定义保持一致，推导后自检符号）。

**B2 反步法**

1. 先做高度通道反步，与 PID 高度对比。
2. 再扩展到水平位置 → 虚拟姿态。
3. 姿态通道反步输出 \(U_2,U_3,U_4\)。
4. 虚控量导数用解析或滤波微分（\(\frac{s}{\tau s+1}\)），禁止裸 `du/dt` 放大噪声。
5. 增益从保守值起：位置环小于姿态环。

**B3 切换与公平对比**

- `algo_id` 常量：同一 `init.m` 切算法，同一 `wp`、同一 `params` 物理段。

### 阶段 C：仿真实验（约 4 学时）

按 E1→E2→E3→E4→E5 顺序，**前一个不稳定不进入下一个**。

每组固定记录：

1. 三维轨迹 vs 期望；
2. \(e_x,e_y,e_z\)；
3. \(\phi,\theta,\psi\) 与 \(\phi_d,\theta_d,\psi_d\)；
4. \(p,q,r\)（看超调）；
5. \(U_1\sim U_4\)（是否饱和）。

E5：保存 `rng` 种子，报告写「随机但可复现」。示波器截图按 PPT 要求放报告。

### 阶段 D：优化与问题闭环（约 3 学时）

**姿态发散排查（必写进「问题与解决」）**

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 起飞即翻 | Mixer 符号/电机转向错 | 开环单轴力矩测试 |
| 越飞越晃 | 内环 P 过大或外环比内环快 | 先降外环 Kp，再加内环 Kd |
| 积分后突然炸 | I 项饱和 | 限幅 + 积分分离（\|e\|大时停积） |
| 航点阶跃后发散 | 位置指令不平滑 | 五次多项式插值 |
| 反步数值爆 | 虚控导数尖峰 | 滤波微分、减小增益 |
| 高度掉/冲顶 | \(U_1\) 限幅或重力补偿错 | 检查 \(U_1=mg/(c\phi c\theta)\) |

**机械臂若误选本线**：末端抖动对应「内环过刚 + 未滤波」；无人机对应项就是角速度超调。报告用误差曲线定位，不要只写“调参后正常”。

**指标优化顺序**：稳定 → 超调 → RMSE → 抗扰。不要一上来追极小误差。

### 阶段 E：可视化与交付（约 2 学时）

- 必要：Scope + `plot3` + 误差图 + 对比表。
- 可选：Aerospace Blockset、FlightGear（PPT 提到即可加分）。
- 提交：`.slx`、`.m`、报告 PDF、图、`README`（如何运行 `init.m`）。

---

## 4. 验收自检清单

- [ ] 手绘：坐标系、受力/反扭矩、控制框图、反步递推图
- [ ] 公式编辑器：运动学、动力学、PID、反步控制律
- [ ] Plant 开环自检通过
- [ ] PID、反步均能 E1–E5
- [ ] 5 航点随机、可复现、平滑切换
- [ ] 双算法同一表对比 RMSE/超调/调节时间
- [ ] 至少复盘 1 次「姿态发散」：曲线 + 原因 + 改参前后
- [ ] 每人报告有个人工作与问题记录（PPT：不可互相抄）

---

## 5. 建议时间表（16 学时）

| 课次 | 产出 |
|---|---|
| 第 1 次（任务发布） | 选题、指标、框图草稿 |
| 第 2 次（中间验收） | Plant + PID 悬停/升降可演示 |
| 第 3 次 | 前后左右 + 反步高度/姿态 |
| 第 4 次 | 五航点、对比表、发散复盘、FlightGear（可选） |
| 课后 1 周 | 报告 + 工程文件 |

中间验收最低线：**PID 能悬停和升降**，否则后续来不及。

---

## 附录：若改选机械臂（仅当组内已定做臂）

方式：MATLAB Robotics System Toolbox，`SerialLink` + `Link([theta,d,a,alpha])`，或 ROS+Gazebo。

步骤：DH 建模 → 正运动学数值验证 → 逆运动学/雅可比 → **关节 PID + 计算力矩/反步** 两种算法 → 直线/圆弧末端跟踪 → 用末端误差与关节速度解释「末端抖动」（降增益、加微分滤波、轨迹加减速）。

PPT 未强制五航点随机位置，但须同样满足「≥2 算法、MATLAB、误差曲线、问题复盘」。
