# 内容二设计方案：移动机器人运动轨迹规划

> 依据 PPT 第 15–20、26 页。闭环：**环境 → 全局规划 → 局部避障 → 对比 → 优化**。  
> 平台：**Python**（动画 + DRL 比 MATLAB 省事；与内容一的 MATLAB 分工明确）。  
> 环境选 **2D 栅格**（搜索过程最好画，验收「一步步展示」硬要求）。

---

## 0. 与 PPT 的一一对应

| PPT 要求 | 本方案落地 |
|---|---|
| 搭建规划仿真环境 | `env/` 栅格地图 + 动态障碍 + 可视化 |
| 经典全局规划 | **A\***（主）+ **Dijkstra**（对照搜索过程） |
| 搜索过程逐步展示 | 开放集/关闭集逐帧染色，再连最短路 |
| 局部规划须 **2 种** | **DWA** + **DQN**（DRL，PPT 点名可选） |
| 动态/未知障碍，写清运动逻辑 | 往复、圆周，见 §2.3 |
| ≥2 条端到端（全局+局部全程） | 管线 A：A\*+DWA；管线 B：A\*+DQN |
| ≥2 张测试地图 | 稀疏、迷宫、狭长通道（做 3 张） |
| MATLAB 或 Python | Python 3 + matplotlib / pygame 可选 |
| 对比优缺点 | 同一地图、同一动态障碍，比成功率/长度/碰撞/耗时 |

内容一已用 MATLAB 交差；内容二用 Python，报告里写清「平台选择理由」即可。

---

## 1. 设计方法

### 1.1 方法总纲

路径规划 = 在有障碍的环境里找一条从起点到终点的可行/较优路径。PPT 三种「优」：最短、最快、最安全。本课以 **最短可行 + 动态安全** 为主。

必须做成 **全局 + 局部协同**（PPT 第 17 页），不能只跑一个 A\*：

```
静态地图 ──► 全局规划器 ──► 宏观折线（指引线）
                                  │
传感器（局部窗口）                 ▼
动态/未知障碍 ──► 局部规划器 ──► 下一步速度/格子
                                  │
                                  ▼
                         机器人移动一拍，循环
```

- 前方无新障碍：局部规划贴着全局折线走。
- 前方出现地图上没有的障碍：局部绕开，绕开后回到全局折线。

### 1.2 全局规划方法

在 **已知、静态** 栅格上搜完整最短路。障碍视为 4 连通或 8 连通。

| 算法 | 做法 | 本课用途 |
|---|---|---|
| Dijkstra | 均匀扩展，保证最短 | 展示「无启发」搜索范围更大 |
| A\* | \(f=g+h\)，\(h\)=曼哈顿/欧氏 | **主全局算法**，搜索过程动画 |
| RRT/PRM | 采样，适合连续空间 | 栅格方案里不作主算法（报告可写为何不选） |

A\* 必须逐步可视化：当前扩展格、开放集、关闭集、最终路径。这是 PPT 硬性验收。

### 1.3 局部规划方法（必须两种）

| 算法 | 类型 | 做法 |
|---|---|---|
| **DWA** | 经典 | 在速度窗口采 \((v,\omega)\)，预测轨迹，打分：贴全局路、避障、朝向目标 |
| **DQN** | DRL（PPT 允许） | 栅格上离散动作（上/下/左/右/停），用局部观测学绕行动作 |

不选 TEB 的原因：优化器重，Python 实现周期长。VFH 可作 DWA 的备选，若 DQN 训不出来再换 VFH。

### 1.4 端到端两条管线（PPT「两种以上」）

| 管线 | 全局 | 局部 | 对比点 |
|---|---|---|---|
| A | A\* | DWA | 无需训练、可解释、动态绕行稳 |
| B | A\* | DQN | 无解析模型、动态交互，但要训练、可复现性差 |

两条都必须：静态图上先 A\* → 沿路走 → 窗口内出现动态障碍 → 局部改道 → 回到 A\* 路径 → 到终点。

### 1.5 评价指标

| 指标 | 定义 |
|---|---|
| 成功率 | 到终点且不撞 |
| 路径长度 | 实际走过的格数/米 |
| 碰撞次数 | 与静/动态障碍接触 |
| 规划耗时 | 全局一次 + 局部平均每拍 |
| 绕行质量 | 离开全局路径的最大偏离、是否回到指引线 |

---

## 2. 设计方式

### 2.1 环境：2D 栅格（二选一里选这个）

格子状态：`0` 可走 / `1` 静态障碍 / `2` 动态障碍 / `S` 起点 / `G` 终点。

建议尺寸：稀疏 `40×40`，迷宫 `30×30`，狭缝 `20×50`。机器人占 1 格（可扩 3×3 膨胀，当安全半径）。

**为何不选连续/物理仿真：** 全局「逐步搜索」在栅格上最好画；DWA 可在栅格上用简化运动（或格子中心 + 朝向离散）。若答辩问连续环境：说明 DWA 评分轨迹已是局部连续近似。

### 2.2 三张测试地图（≥2，做 3）

| 地图 | 特征 | 用来逼哪条 PPT 要求 |
|---|---|---|
| `sparse.txt` | 少量方块障碍，起点左下、终点右上 | 稀疏图 |
| `maze.txt` | 多墙迷宫 | 密集迷宫 |
| `corridor.txt` | 中间一条窄通道，两侧墙 | 狭长通道 |

三张都叠加 **同一套动态障碍逻辑**，保证管线 A/B 可比。

### 2.3 动态障碍运动逻辑（报告必须写）

自己设计、写进报告「方法」：

| 障碍 | 运动 | 参数 |
|---|---|---|
| D1 往复 | 在水平或垂直走廊两端折返，匀速 1 格/拍 | 周期、幅度写进配置 |
| D2 圆周/矩形环 | 绕一块静态障碍转圈 | 半径、角速度 |
| D3（可选） | 沿固定折线巡逻 | 路径点列表 |

规则：

- 动态障碍 **不写入** 全局地图（全局 A\* 当它不存在）→ 满足「未知/突发」。
- 每拍按逻辑更新格子；机器人只在 **局部窗口**（如 7×7 或半径 5 格）里看见它们。
- 与静态障碍碰撞则停一拍或反向（写进说明，避免障碍卡墙）。

### 2.4 DQN 的 MDP（PPT 第 18 页五项，报告按这个写）

| 要素 | 本课定义 |
|---|---|
| 智能体 | 栅格移动机器人 |
| 环境 | 三张地图 + D1/D2 + 终点 |
| 状态 | 局部窗口栅格（静+动）+ 相对终点/相对全局路径最近点 |
| 动作 | 离散：上、下、左、右、停留 |
| 奖励 | 靠近终点 +；撞障大负；贴全局路径小正；每步小负（促最短） |

训练：只在局部避障阶段用 DQN；全局仍用 A\* 给指引。这是 PPT 说的「DRL 与传统混合」，不是纯端到端乱走。

价值网络：DQN（离散动作）。不上一开始就上 PPO。

### 2.5 工程目录

```
内容二_轨迹规划/
  maps/sparse.txt
  maps/maze.txt
  maps/corridor.txt
  env/grid_map.py          # 读地图、膨胀、动态障碍更新
  env/dynamic.py           # D1/D2 运动逻辑
  global_planner/astar.py
  global_planner/dijkstra.py
  local_planner/dwa.py
  local_planner/dqn_agent.py
  pipeline/a_star_dwa.py
  pipeline/a_star_dqn.py
  vis/animate_search.py    # A* 逐步搜索
  vis/animate_run.py       # 整段行驶 + 动态障碍
  train_dqn.py
  run_compare.py           # 3 图 × 2 管线，出表出图
  params.py
  report_figs/
```

### 2.6 报告对应（PPT 报告结构）

| 章节 | 写什么 |
|---|---|
| 目的要求 | 抄任务：全局+双局部、动态障碍、≥2 地图、双管线对比 |
| 原理 | 手绘：栅格、A\* 示意、DWA 速度窗口、DQN 网络；公式编辑器写 \(f=g+h\)、DWA 得分、Bellman |
| 方法 | 需求、两管线框图、障碍运动逻辑、MDP 五要素 |
| 原始记录 | 搜索动画截图、关键代码、超参表 |
| 结果 | 三地图×两管线：轨迹图、成功率表、优缺点 |
| 总结 | 问题（局部最优、DQN 不收敛、狭缝卡住）及解决 |

---

## 3. 设计步骤

### 阶段 A：环境与全局（先过中间验收）

1. 画/导出三张栅格图，膨胀 1 格。
2. 实现 Dijkstra、A\*，同一地图对比扩展格数。
3. **动画：** 每扩展一格刷新开放/关闭集，最后画路径。验收底线：A\* 能逐步搜完稀疏图。
4. 无动态障碍时，机器人沿 A\* 格子走，能到终点。

### 阶段 B：局部 DWA（管线 A）

1. 机器人状态：格坐标 + 朝向（4 或 8 向）。
2. 速度窗口采样，预测未来 \(N\) 步，碰静态/窗口内动态则丢弃。
3. 得分：`α·朝向终点 + β·避障间隙 + γ·贴全局路径`。
4. 加入 D1 往复障碍，确认会停/绕，绕后回到 A\* 线。
5. 三张地图都跑通管线 A。

### 阶段 C：局部 DQN（管线 B）

1. 按 §2.4 写环境 `step/reset`。
2. 先在稀疏图 + 单个往复障碍上训到「会躲且朝目标」。
3. 接 A\* 指引：奖励加「靠近路径点」。
4. 保存权重；`run_compare` 只加载推理，不现场再训。
5. 若 2–3 晚仍不收敛：局部第二算法改 **VFH**，报告写 DQN 失败原因（仍算尝试过 DRL）。

### 阶段 D：对比与优化

1. `run_compare.py`：3 地图 × 2 管线，固定随机种子。
2. 表：成功率、长度、碰撞、耗时。
3. 图：搜索过程、无动态全局路、有动态实际轨迹（标出绕行段）。
4. 已知坑：
   - 迷宫里 DWA 贴墙：加大膨胀或避障权重。
   - 狭缝 + 动态障碍对撞：给动态障碍「可见即减速」或允许机器人停留。
   - U 型陷阱：局部失败则 **触发全局重规划**（窗口发现死路时用当前静态+可见动态重跑 A\*）。这是加分项，建议写进「问题与解决」。

### 阶段 E：交付

- 源码 + `README`（怎么跑搜索动画、怎么跑对比）。
- `report_figs/` 搜索过程、三地图轨迹、对比表。
- 每人报告：个人工作、障碍运动逻辑、双管线优缺点。

---

## 4. 建议时间（16 学时）

| 课次 | 产出 |
|---|---|
| 第 1 次 | 选题（栅格）、三张地图、框图 |
| 第 2 次（中间验收） | A\* 逐步搜索动画 + 无动态走完全程 |
| 第 3 次 | 管线 A（A\*+DWA）+ 动态障碍 |
| 第 4 次 | 管线 B（DQN 或 VFH）+ 三地图对比表 |
| 课后 | 报告、重规划（可选） |

中间验收底线：**A\* 可视化 + 静态图能走到终点**。

---

## 5. 验收自检

- [ ] 手绘：全局/局部协同图、A\* 节点、DWA 窗口、DQN 结构
- [ ] 公式：\(f=g+h\)、DWA 代价、DQN 损失
- [ ] 搜索过程逐步演示
- [ ] 两种局部规划器
- [ ] 两条端到端管线都能到终点
- [ ] ≥2 张地图（建议 3 张）
- [ ] 动态障碍运动逻辑写进报告
- [ ] 对比表：优缺点（A 可解释、B 适应性；A 狭缝稳、B 要训练）

---

## 6. 和内容一的衔接（报告一句话）

内容一解决「给定航点如何稳飞」；内容二解决「航点/路径从哪来」。全局路径相当于内容一的航点序列，局部避障相当于航点之间的在线修正。
